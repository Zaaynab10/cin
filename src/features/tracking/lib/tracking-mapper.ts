// Expected: Mapping layer between backend payloads and tracking UI model.
import type { TrackingResult } from "../types/tracking.types";

export function mapTrackingResult(input: TrackingResult): TrackingResult {
	return input;
}
